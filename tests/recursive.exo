// resursive calls
def fib(n) {
    if (n < 2) {
        return n;
    } else {
        return fib(n-2) + fib(n-1);
    }
}

sequence = [];
for (i = 0 to 10) {
    sequence = sequence + fib(i);
}


print(sequence);